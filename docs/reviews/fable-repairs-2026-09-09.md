# Fable review repairs

Review thread: `e2bd0bf3-5c26-43e8-98a7-bdb54e3aca64`. Fable consolidated all eight reports on September 9, 2026. This repair pass starts from `6ffbe578` on master. The owner authorized fixes, verification, packaging, launcher installation and pushing origin/master.

## Verified and repaired

| Finding | Repair and evidence |
|---|---|
| Upstream sends `settledOverride: active`; local schema rejected the whole shell. Unsettle omitted its required reason. | Corrected contract and explicit user reason; engine-client regression checks both snapshots and dispatch. |
| Usage snapshots were treated as patches, retaining expired limits. One provider's reporting disabled probes for others. | Complete successful snapshots replace windows; failed probes preserve previous readings; reporting is per provider. Account regressions check removed model limits and independent probes. |
| Old asynchronous questions fell outside the 500-event cap; stale failures remained answerable. | Retain unresolved asynchronous requests and recognize explicit stale/unknown retirement. Unit coverage exceeds the cap. |
| An uncertain native response could block maintenance forever after another answer or removal. | Match receipts separately from retirement; retirement releases the pending record without claiming delivery. Explicit removal cancels the local retry. Empty attachment groups are normalized. Lost-receipt, retirement and cancellation regressions retain unrelated held comments. |
| Failed Send silently resent old content and then erased newer edits. | Persist and show the original retry payload; preserve later text/files after delivery; reconcile a receipt after reconnect. Discard cancels pending local delivery and restores associated held comments. Binary attachment and draft regressions check exact bytes and preserved edits. |
| Native response review allowed edits that retry ignored. | Show frozen original answer/files, disable edits, and allow removal from Send before editing again. Multi-select answers preserve arrays and provider option values. |
| Attachment counts differed among composer, visual comments and native answers. | Each view includes other held files in the same capacity calculation. Existing eight-file boundary coverage remains. |
| Carried model settings could include obsolete options; built-in model edits were ignored upstream. | Filter implicit/default options using current descriptors; validate explicit settings. Only custom model entries offer editing. New conversations derive models from current engine state and fetch Git refs only for worktree defaults. |
| Compaction could remain working forever after socket loss. | Mark interrupted compaction actionable; later correlated completion can still supersede the interruption. Compaction is excluded from ordinary restart continuation. |
| Recovery notices remained indefinitely, overstated new-turn provenance and ignored restart preference for network changes. | Give recovery and compaction notices distinct React keys so reconnect cannot duplicate them; clear finished recovery, describe observed new work neutrally, permit explicit continuation while waiting, use fresh shell state before maintenance and honor the continuation preference for network changes. |
| Paths and dollar amounts with no matching command swallowed Enter. | Fall through to ordinary Send; skills display their canonical dollar token; command selection styling is unambiguous. |
| Large files had upload/download deadlines suited to small messages. | Both byte transfers allow two minutes while retaining size and destination checks. |
| HTML previews repeatedly detached their native view, local-path IPC was unnecessary, and staged preview files could be swept. | Reuse the attached view, deduplicate bounds reports, restrict sources to verified staged/attached files, retain open staged files and reuse external preview exports. |
| Browser capture continued after owner takeover; paused tabs could not stop recording. | Pause screencast capture on takeover, publish recording state, allow stopping while paused and discard a recording whose start raced takeover. |
| Snapshot files/cards grew without bounds; video preview copied large base64 messages; oversized recording discarded all work. | Bound automatic screenshot retention; read saved media through the existing local protocol; stage annotation bytes in main; stop recording near the transfer cap and retain the completed portion. |
| Window chooser encoded every full-size window and Linux captured stale thumbnails. | Keep small chooser thumbnails, capture the selected X11 window again, preserve the single system-picker selection and bound generated filenames. |
| Captured accessibility text was sent without review; capture layout exceeded small windows and ignored pane zoom. | Show captured text with an inclusion checkbox; use viewport bounds and conversation text scaling. |
| Successful imports with unsupported/skipped sessions were shown as failed and offered futile retries. | Show skips as information after completion; retain retry for actual import errors. |
| Overlapping subscription setup could overwrite a live reader after an await. | Recheck socket, entry, reader and current subscription need after stream creation; interrupt superseded readers. |
| Staged replay validation assumed relative Effect imports and omitted positive page limits. | Resolve bare Effect exports from the actual staged engine; check limits on either side of the page boundary and repeated consumption. |

## Findings not expanded into repairs

The engine replay backport itself was verified as correct. Its actual packaged runtime still needs the package and replay checks recorded with this change. Speculative future enum/contract expansion, unused schema fields, unsupported image policy, reset-credit policy and a different definition of what counts as a draft are separate design changes without a demonstrated failure here. Broad retention redesigns and native platform claims are outside the evidence available from this Linux host. Exported document copies are reused within an open preview and cleaned on shutdown; this pass does not redesign their lifetime across an external viewer.

## Verification record

Task `fable-repairs-20260909` under `~/.cache/stratamd-verification/` retains every attempt, logs, screenshots, traces, input identities and final results. Early focused failures included obsolete behavior expectations, a draft-cache invalidation regression and new test fixture typing errors. Those are corrected before the final gate. No retries, longer test budgets or reduced workstation concurrency are used to obtain signoff. Final gate, managed-runtime, stress and package outcomes are reported in the completion reply and their task record.

The owner's preexisting README change and untracked release/review documents are outside this repair commit. The running owner app is not restarted; the launcher is updated to the newly built package.

## Full-suite follow-up

The first full gate (`2026-09-09T09-05-55-169Z-f850fd19`) passed 1,157 unit/integration tests and found three Electron failures. The answer-file test called Playwright's `setInputFiles` on a disabled second field before the first file finished staging. It now observes staging completion and the next field's enabled state before opening that picker.

The offline Send trace recorded no main-process request for its second preview. Continuous unrelated thread broadcasts recreated the same request object and repeatedly cancelled its 120-millisecond debounce. A controlled test (`2026-09-09T09-16-04-031Z-a669b0a2`) reproduced the same permanently busy preview. Send now preserves request identity while its values are unchanged; actual document content, annotations, draft selections and delivery state still refresh the preview. `send-preview-live-updates.spec.ts` checks both cases while broadcasts continue. The earlier diagnostic attempt (`2026-09-09T09-14-16-618Z-f92067af`) never reached that mechanism because the fixture had no document change to enable Send; the corrected fixture makes a real edit first. All attempts retain their original evidence.

The third failure was an obsolete capacity expectation in the combined visual/native file test. The repaired composer includes held answer files immediately and refuses a ninth file at staging. The test now checks that earlier refusal, removes one held answer file, and verifies the resulting eight original uploads. The second full attempt was cancelled once this known obsolete assertion was identified, before restarting the final gate.

The first stress attempt (`2026-09-09T09-25-28-672Z-bdcbecbf`) exposed three test mechanisms before being stopped for correction. The reconnect trace shows automatic recovery removing the offline button during its click; recovery checks now await the resulting notice without racing a redundant manual reconnect. The large queued-delivery scenario spent its budget repeating thread navigation and driving a large document through the Docs menu, then timed out during its second launch. It now attaches once and closes through the public document API, retaining the original 300 KB payload, disk-save, queue identity and restart assertions. Menu behavior remains covered by the other close scenarios. The answer-files test completed its retry and byte assertions before expiring during its fourth preview. Preview behavior and restart/partial-upload persistence now run as separate tests with all original assertions. CPU pressure was inspected; no memory pressure was present. Timeouts, worker counts and retries remain unchanged.

The second stress attempt (`2026-09-09T09-37-00-035Z-ad6ddb2c`) passed the repaired delivery test in 26.4 seconds, then exposed the same cumulative-budget issue in the combined cross-agent exclusion/inclusion scenario. Its second Send completed at the deadline, leaving only 0.1 seconds for the delivery assertion. The two policies now have independent scenarios; the explicit-inclusion case still creates and attaches the third agent through Projects. All payload-content assertions remain.

The same stress run found the keyboard thread scenario spending its budget on repeated Tab traversals. Its local helper blurred the current control before every target, restarting navigation across the shell instead of continuing from the user's position. It now retains current focus and immediately accepts an already-focused target; all actual key presses, reachability, reply persistence and resolution assertions remain. The saved trace reaches the reply and sends it before the old budget expires, with no stalled product operation.

A third failure in that run was the decision-anchor test reading persisted annotations immediately after clicking Add. The saved result contains the first two decisions while the third creation is still pending. Like the existing Reopen assertion in the same file, the creation assertion now waits for its specific persisted result; it still requires the exact shifted heading and prompt.

The third stress run (`2026-09-09T09-55-10-016Z-15d1e59c`) exposed a large-file fixture ordering issue. The generic launch helper's five-second state poll expired while the 2 MB document was still opening, before the test reached its existing 30-second visual-readiness check. This case now launches the empty shell and awaits the public open operation before checking visual readiness. The measured readiness still includes app startup and opening the original file; all review, edit, upload and byte-preservation assertions remain. No timeout was increased.

The fourth stress run (`2026-09-09T10-12-08-923Z-3a116b1c`) passed the large-file correction, then found a null coordinate read in the annotation handle test. The trace observes the handle as visible and then receives no bounding box during the subsequent layout read. The test now uses its existing `settledBox` helper for that grip too, requiring visibility and matching bounds across animation frames before mouse coordinates are used. The stored quote and displayed highlight assertions are unchanged.

That run also found a real notification race. The previous bulk-close notice was shown at trace time 847943.938 ms; its 2800 ms expiry coincided with the next close notice around 850743 ms. Its unqualified dismissal callback could clear the newer notice before React cleaned up the older effect. A controlled reproduction (`2026-09-09T10-28-58-541Z-6b645dca`) holds the old expiry, shows the new notice, then fires the old callback and reproduces the loss. Toast dismissal now carries the originating notice ID and clears only that notice, including delayed Escape callbacks and action buttons. The regression retains the new notice after the stale callback.

The fifth stress run (`2026-09-09T10-30-44-573Z-1c3974b5`) found a theme color reverting after it had visibly applied. Theme watching compared disk against the newest queued write, then adopted a reload across an await even when a newer local edit arrived. Controlled integration run `2026-09-09T10-46-01-955Z-d1b6392a` holds a disk reload, commits a newer border color, and reproduces that color being overwritten when the stale reload resumes. Theme watching now waits for queued writes, records bytes as its own only after the write completes, and rejects reloads superseded by an edit, selection or write. External edits and deletion/broken-file behavior retain their existing integration coverage.

The sixth stress run (`2026-09-09T10-49-42-130Z-9c80a673`) found a state-read crash while following a renamed open file. The document session moved before tab renaming awaited path canonicalization, leaving the old tab temporarily without its session. Controlled integration run `2026-09-09T10-58-37-516Z-868d4f1b` reads at that precise asynchronous boundary and reproduces the exception. The store already resolves the destination; the session map and tab registry now adopt that canonical path synchronously before yielding. The regression checks the complete view during the transition, and existing checks retain one ghost entry and the same attachments across rename.

The seventh stress run (`2026-09-09T11-07-50-961Z-d87a9476`) exposed the same initial empty-session window in an ordinary review-heavy fixture. Startup opened command-line documents only after creating and loading the renderer, so its first state could remain empty during document initialization. Command-line documents now open after restored tabs but before the first window is created, matching the existing restoration ordering. The large-file check returns to its original command-line launch too. No test startup timeout, worker count or retry setting changes.

The eighth stress run (`2026-09-09T11-25-41-298Z-fe2d4862`) reached the combined native-answer/visual send's five-second dispatch deadline while its saved screen still showed the delivery uploading; the native answer had already been accepted. This assertion timed the entire sequence of eight durable uploads plus dispatch. The test now observes native-response completion and each upload's progress before asserting dispatch, retaining the existing per-check and overall budgets and all exact file-count/byte assertions. Upload and command diagnostics are attached even on failure.

Two other checks in that run exhausted their 30-second budgets while progressing through combined collaboration scenarios. The mode-3 round now ends after its saved change is kept; reporting the denied Lead claim on the next delivery has a separate scenario. Cancelling detach with a queued delivery and detaching without queued work also run independently. All original state, payload and ignored-action assertions remain. Resource inspection found CPU/I/O contention during the prescribed stress load, without memory pressure; worker counts and timeouts remain unchanged.
