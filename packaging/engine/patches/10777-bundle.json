{
  "file": "node_modules/t3/dist/bin.mjs",
  "beforeSHA256": "6e7a9fe68158c677f5d671eaa5267d21af020b38861309c614820d785a1f591a",
  "afterSHA256": "3dcd7ca199e1432d4efbaa7a111566ca779af966c7642fe94d91df3b061385a3",
  "import": "import { paginate as strataReplayPaginate } from \"effect/Stream\";\n",
  "replacements": [
    {
      "before": "\tconst readFromSequence = (sequenceExclusive, limit = DEFAULT_READ_FROM_SEQUENCE_LIMIT) => {\n\t\tconst normalizedLimit = Math.max(0, Math.floor(limit));\n\t\tif (normalizedLimit === 0) return empty$15;\n\t\tconst readPage = (cursor, remaining) => fromEffect(readEventRowsFromSequence({\n\t\t\tsequenceExclusive: cursor,\n\t\t\tlimit: Math.min(remaining, READ_PAGE_SIZE)\n\t\t}).pipe(mapError(toPersistenceSqlOrDecodeError$4(\"OrchestrationEventStore.readFromSequence:query\", \"OrchestrationEventStore.readFromSequence:decodeRows\")), flatMap$1((rows) => forEach(rows, (row) => decodeEvent(row).pipe(mapError(toPersistenceDecodeError(\"OrchestrationEventStore.readFromSequence:rowToEvent\"))))))).pipe(flatMap$3((events) => {\n\t\t\tif (events.length === 0) return empty$15;\n\t\t\tconst nextRemaining = remaining - events.length;\n\t\t\tif (nextRemaining <= 0) return fromIterable$2(events);\n\t\t\treturn concat$1(fromIterable$2(events), readPage(events[events.length - 1].sequence, nextRemaining));\n\t\t}));\n\t\treturn readPage(sequenceExclusive, normalizedLimit);\n\t};\n",
      "after": "\tconst readFromSequence = (sequenceExclusive, limit = DEFAULT_READ_FROM_SEQUENCE_LIMIT) => {\n\t\tconst normalizedLimit = Math.max(0, Math.floor(limit));\n\t\tif (normalizedLimit === 0) return empty$15;\n\t\treturn strataReplayPaginate({ cursor: sequenceExclusive, remaining: normalizedLimit }, ({ cursor, remaining }) => readEventRowsFromSequence({\n\t\t\tsequenceExclusive: cursor,\n\t\t\tlimit: Math.min(remaining, READ_PAGE_SIZE)\n\t\t}).pipe(mapError(toPersistenceSqlOrDecodeError$4(\"OrchestrationEventStore.readFromSequence:query\", \"OrchestrationEventStore.readFromSequence:decodeRows\")), flatMap$1((rows) => forEach(rows, (row) => decodeEvent(row).pipe(mapError(toPersistenceDecodeError(\"OrchestrationEventStore.readFromSequence:rowToEvent\"))))), map$5((events) => {\n\t\t\tconst last = events.at(-1);\n\t\t\tconst nextRemaining = remaining - events.length;\n\t\t\treturn [events, last === undefined || nextRemaining <= 0 ? none$1() : some({ cursor: last.sequence, remaining: nextRemaining })];\n\t\t})));\n\t};\n"
    },
    {
      "before": "\tconst readAggregateRange = (input) => {\n\t\tconst limit = Math.max(0, Math.floor(input.limit ?? DEFAULT_READ_FROM_SEQUENCE_LIMIT));\n\t\tif (limit === 0 || input.fromSequenceExclusive >= input.toSequenceInclusive) return empty$15;\n\t\tconst readPage = (cursor, remaining) => fromEffect(readAggregateEventRows({\n\t\t\t...input,\n\t\t\tfromSequenceExclusive: cursor,\n\t\t\tlimit: Math.min(remaining, READ_PAGE_SIZE)\n\t\t}).pipe(mapError(toPersistenceSqlOrDecodeError$4(\"OrchestrationEventStore.readAggregateRange:query\", \"OrchestrationEventStore.readAggregateRange:decodeRows\")), flatMap$1((rows) => forEach(rows, (row) => decodeEvent(row).pipe(mapError(toPersistenceDecodeError(\"OrchestrationEventStore.readAggregateRange:rowToEvent\"))))))).pipe(flatMap$3((events) => {\n\t\t\tconst last = events.at(-1);\n\t\t\tif (last === void 0) return empty$15;\n\t\t\tconst nextRemaining = remaining - events.length;\n\t\t\tif (events.length < READ_PAGE_SIZE || nextRemaining === 0 || last.sequence >= input.toSequenceInclusive) return fromIterable$2(events);\n\t\t\treturn concat$1(fromIterable$2(events), readPage(last.sequence, nextRemaining));\n\t\t}));\n\t\treturn readPage(input.fromSequenceExclusive, limit);\n\t};\n",
      "after": "\tconst readAggregateRange = (input) => {\n\t\tconst limit = Math.max(0, Math.floor(input.limit ?? DEFAULT_READ_FROM_SEQUENCE_LIMIT));\n\t\tif (limit === 0 || input.fromSequenceExclusive >= input.toSequenceInclusive) return empty$15;\n\t\treturn strataReplayPaginate({ cursor: input.fromSequenceExclusive, remaining: limit }, ({ cursor, remaining }) => readAggregateEventRows({\n\t\t\t...input,\n\t\t\tfromSequenceExclusive: cursor,\n\t\t\tlimit: Math.min(remaining, READ_PAGE_SIZE)\n\t\t}).pipe(mapError(toPersistenceSqlOrDecodeError$4(\"OrchestrationEventStore.readAggregateRange:query\", \"OrchestrationEventStore.readAggregateRange:decodeRows\")), flatMap$1((rows) => forEach(rows, (row) => decodeEvent(row).pipe(mapError(toPersistenceDecodeError(\"OrchestrationEventStore.readAggregateRange:rowToEvent\"))))), map$5((events) => {\n\t\t\tconst last = events.at(-1);\n\t\t\tconst nextRemaining = remaining - events.length;\n\t\t\treturn [events, last === undefined || events.length < READ_PAGE_SIZE || nextRemaining === 0 || last.sequence >= input.toSequenceInclusive ? none$1() : some({ cursor: last.sequence, remaining: nextRemaining })];\n\t\t})));\n\t};\n"
    }
  ]
}
